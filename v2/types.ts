export type OptionKey = string | number | boolean;

// eslint-disable-next-line import/prefer-default-export
export enum Keys {
    Tab = 9,
    Esc = 27,
    Enter = 13,
    Down = 38,
    Up = 40,
}
